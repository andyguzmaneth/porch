[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

# Porch V1 — on-chain registry. `forge test` to run; `anvil` + the deploy script for a local chain.
